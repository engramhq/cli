{
  "name": "",
  "version": "1.0.0",
  "scripts": {
    "compile": "tsc --noEmit",
    "build": "esbuild server.ts --platform=node --bundle --outfile=built/server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongodb": "^5.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.16",
    "esbuild": "^0.17.5",
    "typescript": "^4.9.5"
  }
}
